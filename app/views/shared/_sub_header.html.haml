%nav.navbar.navbar-dark.bg-dark.mypage_header
  .pull-right
    = link_to "/squares",{class: "ml-3 header_btn_color"} do
      .fas.fa-tasks
      目標管理
    - unless current_user.groups.empty?
      = link_to group_plans_path(current_user.groups.first),{class: "ml-3 header_btn_color"} do
        .fas.fa-columns
        予定
      = link_to "/diaries",{class: "ml-3 header_btn_color"} do
        .fas.fa-book
        diary
    - if current_user.groups.empty?
      = link_to new_group_path,{class: "ml-3 header_btn_color"} do
        .fas.fa-users
        group
    - else
      = link_to group_path(current_user.groups.first),{class: "ml-3 header_btn_color"} do
        .fas.fa-users
        group
    = link_to user_path(current_user), {class: "ml-3 header_btn_color"} do
      .fas.fa-user-alt
      profile
  .pull-left
.space_header
