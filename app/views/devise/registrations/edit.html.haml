.form_frame
  %h2
    Edit #{resource_name.to_s.humanize}
  = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
    = render "devise/shared/error_messages", resource: resource
    .form-group
      = f.label :メールアドレス
      %br/
      = f.email_field :email, autofocus: true, autocomplete: "email",class: "form-control"
    - if devise_mapping.confirmable? && resource.pending_reconfirmation?
      %div
        Currently waiting confirmation for: #{resource.unconfirmed_email}
    .form-group
      = f.label :ニックネーム
      %em
        (10 文字まで)
      %br/
      = f.text_field :nickname, autocomplete: "nickname",class: "form-control",maxlength: "10"
    .form-group
      = f.label :パスワード
      - if @minimum_password_length
        %em
          (
          = @minimum_password_length
          文字以上)
      %i (パスワードを変更する場合は入力してください)
      %br/

      = f.password_field :password, autofocus: true, autocomplete: "new-password",class: "form-control"

    .form-group
      = f.label :パスワード確認
      %br/
      = f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control"
    .form-group
      = f.label :現在のパスワード
      %i (ユーザー情報の変更のため入力してください)
      %br/
      = f.password_field :current_password, autocomplete: "current-password", class: "form-control"

    %div
      = f.submit "更新",class:"btn btn-primary"
  %br
  %h3 ユーザーの削除
  %p
    ※一度削除したユーザーの情報は復元できません。 #{button_to "このユーザーを削除する", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete,class:"btn btn-primary"}
  = link_to "戻る", :back,class:"btn btn-primary"
